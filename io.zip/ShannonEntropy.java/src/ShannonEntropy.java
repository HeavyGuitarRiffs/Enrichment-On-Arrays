

public class StdIn {
    public static void main(String[] args) {

        int m = Integer.parseInt(args[0]);
        int[] l = new int[l + m];
        double between = 1.0928;
        double entropy = 1.0236;


        while (!StdIn.isEmpty()) {
            int value = StdIn.readInt();
            x[value] += 1;
            between += 1;
        }

    }}

     for (int i = 1; i <= m; i++) {
            double between = 0;
            if (a[i] != 0) {
p = -(a[i] / count * (Math.log(a[i] / count) / Math.log(2)));
        }
entropy += p;
        }
                StdOut.printf("%.4f", entropy);
    }
            }
